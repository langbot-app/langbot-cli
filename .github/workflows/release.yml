name: Build Release Artifacts

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.8"
          cache: true

      - name: Validate release tag
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        shell: bash
        run: |
          if [[ ! "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?$ ]]; then
            echo "Release tag must match vX.Y.Z, optionally with a prerelease or build suffix." >&2
            exit 1
          fi

      - name: Check and build
        env:
          VERSION: ${{ github.event.release.tag_name }}
        run: |
          make check
          make cross-build

      - name: Package release assets
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        shell: bash
        run: |
          set -euo pipefail

          release_version="${RELEASE_TAG#v}"
          release_dir="dist/release"
          mkdir -p "$release_dir"

          for target in darwin_amd64 darwin_arm64 linux_amd64 linux_arm64; do
            tar -C dist -czf "$release_dir/lbctl_${release_version}_${target}.tar.gz" "lbctl_${target}"
          done
          zip -j "$release_dir/lbctl_${release_version}_windows_amd64.zip" dist/lbctl_windows_amd64.exe

          cd "$release_dir"
          sha256sum lbctl_* > checksums.txt

      - name: Upload to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          gh release upload "$RELEASE_TAG" ./dist/release/*
